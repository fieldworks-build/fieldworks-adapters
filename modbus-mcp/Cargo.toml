[package]
name = "modbus-mcp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "MCP server exposing Modbus TCP as a FieldWorks nine-tool industrial protocol adapter"
keywords = ["mcp", "modbus", "iot", "industrial", "scada"]
categories = ["network-programming", "hardware-support"]

[dependencies]
# path (local workspace builds) + version (once published) — see fieldworks-adapters#3.
fieldworks-adapter-core = { path = "../fieldworks-adapter-core", version = "1.0" }
rmcp = { version = "3", features = ["transport-io", "schemars"] }
tokio-modbus = { version = "0.17", default-features = false, features = ["tcp"] }
tokio = { version = "1", features = ["rt-multi-thread", "net", "io-util", "time", "macros", "sync"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
